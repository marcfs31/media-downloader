## What & why

<!-- The change, and the reason for it — not just a restatement of the diff. -->

## Correctness gate

- [ ] `./scripts/run-checks.sh` passes locally (same gate CI runs)
- [ ] New/changed behavior has test coverage, or an explanation of why not
- [ ] Reviewed with the multi-angle swarm (`/swarm` → review pattern) or
      `/code-review` for anything touching auth, data, money, or public API surface

## Notes for reviewers

<!-- Anything a reviewer should know that isn't obvious from the diff: scope left
     out on purpose, a tradeoff you're not fully sure about, a follow-up you intend
     to file. -->
