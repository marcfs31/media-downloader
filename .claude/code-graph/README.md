# Code-structure graph (generated)

`graph.json` in this directory is produced by `scripts/build-code-graph.sh`. It is a
cache, not hand-maintained content — do not edit it directly, and don't rely on it
being present in a fresh checkout (it's gitignored; run the script or `/graph-update`
to (re)generate it).

See `.claude/skills/code-graph/SKILL.md` for how to read and refresh it.
